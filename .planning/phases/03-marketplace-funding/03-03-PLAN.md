---
phase: 03-marketplace-funding
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - src/lib/marketplace/actions.ts
  - src/hooks/use-marketplace-realtime.ts
  - src/components/marketplace/funding-progress-bar.tsx
  - src/components/marketplace/expected-return-preview.tsx
  - src/components/marketplace/realtime-status.tsx
  - tests/marketplace/realtime.test.ts
autonomous: true
requirements:
  - FUND-02
  - FUND-03
  - FUND-04
  - FUND-05
must_haves:
  truths:
    - "Investor purchase submissions go through one validated server action that calls the funding RPC, not direct table mutations."
    - "The live funding layer prefers Supabase subscriptions and visibly degrades to polling fallback when channel health drops."
    - "Progress and expected return UI primitives consume the shared calculation contracts rather than duplicating business math."
  artifacts:
    - path: "src/lib/marketplace/actions.ts"
      provides: "Server action boundary for purchases"
    - path: "src/hooks/use-marketplace-realtime.ts"
      provides: "Realtime subscription + polling fallback state machine"
    - path: "tests/marketplace/realtime.test.ts"
      provides: "Regression coverage for subscription/fallback behavior"
  key_links:
    - from: "src/lib/marketplace/actions.ts"
      to: "public.fund_invoice"
      via: "validated server action RPC call"
      pattern: "supabase.rpc('fund_invoice'"
    - from: "src/hooks/use-marketplace-realtime.ts"
      to: "src/lib/marketplace/queries.ts"
      via: "polling fallback refresh"
      pattern: "2-second polling"
    - from: "src/components/marketplace/expected-return-preview.tsx"
      to: "src/lib/marketplace/calculations.ts"
      via: "shared return math"
      pattern: "expected return"
---

<objective>
Wire the purchase orchestration and live-update foundation the UI will sit on: server action boundary, realtime hook, and reusable funding display primitives.

Purpose: keep funding writes secure and make realtime/polling behavior testable before page-level UI is built.
Output: purchase action, realtime fallback hook, and shared progress/return components.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-marketplace-funding/03-RESEARCH.md
@src/lib/supabase/server.ts
@src/lib/supabase/client.ts
@playwright.config.ts
@.planning/phases/03-marketplace-funding/03-01-PLAN.md
@.planning/phases/03-marketplace-funding/03-02-PLAN.md

<interfaces>
From src/lib/supabase/client.ts:
```typescript
export function createSupabaseBrowserClient() {
  return createBrowserClient(url, key)
}
```

From 03-01 contract:
```typescript
export type MarketplaceInvoiceCard = {
  id: string
  riskTier: 'A' | 'B' | 'C' | 'D'
  discountRate: number
  fundedFractions: number
  totalFractions: number
  availableFractions: number
}

export type InvoiceFundingSnapshot = MarketplaceInvoiceCard & {
  perFractionNetAmount: number
  perFractionExpectedReturn: number
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the purchase action and realtime fallback state machine</name>
  <files>src/lib/marketplace/actions.ts, src/hooks/use-marketplace-realtime.ts, tests/marketplace/realtime.test.ts</files>
  <behavior>
    - Test 1: purchase input validation rejects missing invoice IDs, non-positive counts, and non-investor sessions before any RPC call.
    - Test 2: the realtime hook stays in `live` mode when the Supabase channel reaches `SUBSCRIBED` and patches invoice progress from invoice updates.
    - Test 3: when the channel errors, closes, or never subscribes, the hook enters polling mode and refreshes snapshots every 2 seconds until recovery.
  </behavior>
  <action>Create `src/lib/marketplace/actions.ts` with a server action that validates `{ invoiceId, fractionCount }` via Zod, confirms an authenticated investor session, and calls `supabase.rpc('fund_invoice', ...)` as the only purchase write path. Then build `src/hooks/use-marketplace-realtime.ts` around the browser client so dashboard pages subscribe primarily to `public.invoices` updates, optionally invoice-scoped `public.fractions` updates for detail views, and visibly fall back to 2-second polling when the channel is unhealthy. The realtime test suite must cover progress patching and fallback transitions without relying on manual browser inspection.</action>
  <verify>
    <automated>npx vitest run tests/marketplace/realtime.test.ts tests/invoices/fund-invoice.test.ts</automated>
  </verify>
  <done>Funding writes flow through one validated server action and the live-update hook can recover cleanly to polling fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Build shared funding UI primitives for progress, returns, and channel status</name>
  <files>src/components/marketplace/funding-progress-bar.tsx, src/components/marketplace/expected-return-preview.tsx, src/components/marketplace/realtime-status.tsx</files>
  <action>Create reusable components that the dashboard and invoice detail page can share: a funding progress bar showing percentage plus sold/total fractions, an expected-return preview showing purchase total + expected return + interest for a selected count, and a compact realtime-status chip that distinguishes `Live` from `Fallback cada 2s`. Consume the helpers from `src/lib/marketplace/calculations.ts`; do not recompute any pricing or progress inline in JSX.</action>
  <verify>
    <automated>npm run build && npx vitest run tests/marketplace/progress.test.ts tests/marketplace/returns.test.ts tests/marketplace/realtime.test.ts</automated>
  </verify>
  <done>The core funding widgets are reusable across marketplace cards and detail screens and remain aligned with tested business logic.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/marketplace/realtime.test.ts tests/invoices/fund-invoice.test.ts`
- `npm run build`
</verification>

<success_criteria>
- Purchases are initiated only through a validated server action that calls `fund_invoice`.
- Realtime behavior is primary and polling fallback is explicit, tested, and visible in the UI.
- Shared funding widgets exist before page assembly begins.
</success_criteria>

<output>
After completion, create `.planning/phases/03-marketplace-funding/03-03-SUMMARY.md`
</output>
