---
phase: 04-settlement-dashboards-demo-polish
plan: 04
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - src/components/dashboard/metric-card.tsx
  - src/components/dashboard/transaction-history-table.tsx
  - src/components/dashboard/portfolio-breakdown.tsx
  - src/app/(cedente)/cedente/dashboard/page.tsx
  - src/app/(inversor)/inversor/dashboard/page.tsx
autonomous: true
requirements:
  - USER-02
  - USER-03
  - SETT-03
must_haves:
  truths:
    - "Cedente dashboard shows invoice status counts, total capital raised, effective financing cost, and recent transaction history."
    - "Investor dashboard keeps the marketplace landing but adds portfolio holdings, weighted average yield, diversification, and recent transactions."
    - "Transaction history is visible on the main dashboards without inventing new ledger tables."
  artifacts:
    - path: "src/components/dashboard/metric-card.tsx"
      provides: "Shared KPI presentation for both roles"
    - path: "src/components/dashboard/transaction-history-table.tsx"
      provides: "Shared ledger-history renderer"
    - path: "src/components/dashboard/portfolio-breakdown.tsx"
      provides: "Investor diversification / payer mix UI"
    - path: "src/app/(cedente)/cedente/dashboard/page.tsx"
      provides: "Real cedente dashboard with Phase 4 metrics"
    - path: "src/app/(inversor)/inversor/dashboard/page.tsx"
      provides: "Marketplace landing enriched with investor portfolio metrics"
  key_links:
    - from: "src/app/(cedente)/cedente/dashboard/page.tsx"
      to: "src/lib/settlement/queries.ts"
      via: "cedente KPI and history query"
      pattern: "getCedenteDashboard"
    - from: "src/app/(inversor)/inversor/dashboard/page.tsx"
      to: "src/lib/settlement/queries.ts"
      via: "investor KPI and history query"
      pattern: "getInvestorDashboard"
    - from: "src/app/(inversor)/inversor/dashboard/page.tsx"
      to: "src/components/marketplace/marketplace-grid.tsx"
      via: "stable investor landing route with added portfolio sections"
      pattern: "MarketplaceGrid"
---

<objective>
Replace placeholder dashboards with demo-ready role-specific metrics while keeping the established landing-route contracts intact.

Purpose: make the happy path legible from each user's home screen after funding and settlement occur.
Output: shared dashboard components plus upgraded cedente and investor dashboards with transaction history.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-settlement-dashboards-demo-polish/04-RESEARCH.md
@src/app/(cedente)/cedente/dashboard/page.tsx
@src/app/(inversor)/inversor/dashboard/page.tsx
@src/components/marketplace/marketplace-grid.tsx

<interfaces>
From src/lib/settlement/queries.ts (from Plan 04-02):
```typescript
export async function getCedenteDashboard(): Promise<{
  statusCounts: Record<string, number>
  totalCapitalRaised: number
  effectiveFinancingCost: number
  spreadTotal: number
  recentTransactions: Array<{ id: string; type: string; amount: number; at: string; description: string }>
  invoices: Array<{ id: string; invoiceNumber: string; status: string; amount: number; netAmount: number; dueDate: string }>
}>

export async function getInvestorDashboard(): Promise<{
  holdings: Array<{ invoiceId: string; invoiceNumber: string; payerName: string; payerCuit: string; ownedFractions: number; investedPrincipal: number; expectedReturn: number; realizedReturn: number; status: string }>
  weightedAverageYield: number
  diversificationCount: number
  payerBreakdown: Array<{ payerCuit: string; payerName: string; investedPrincipal: number; share: number }>
  recentTransactions: Array<{ id: string; type: string; amount: number; at: string; description: string }>
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared KPI, ledger-history, and diversification components</name>
  <files>src/components/dashboard/metric-card.tsx, src/components/dashboard/transaction-history-table.tsx, src/components/dashboard/portfolio-breakdown.tsx</files>
  <action>Create presentation-only dashboard components that can render the read models from Plan 04-02 without additional shaping: KPI cards with numeric formatting, a transaction history table that supports purchases/disbursements/returns, and an investor portfolio breakdown that highlights distinct payers and concentration. Keep them reusable across both role dashboards and mobile-safe for the later Playwright/MCP verification pass.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The repo has shared dashboard primitives ready to render Phase 4 metrics and ledger history across desktop and mobile.</done>
</task>

<task type="auto">
  <name>Task 2: Ship the role-specific dashboard pages with real Phase 4 metrics</name>
  <files>src/app/(cedente)/cedente/dashboard/page.tsx, src/app/(inversor)/inversor/dashboard/page.tsx</files>
  <action>Replace the cedente placeholder dashboard with real Phase 4 data: invoice status grouping, total capital raised, effective financing cost, spread total, owned invoice list, and recent ledger entries. Keep `/inversor/dashboard` as the landing route, add investor KPI sections above/below the existing `MarketplaceGrid`, and render holdings, weighted average yield, diversification, and transaction history without removing the marketplace. Reuse `DashboardHero` and the shared components from Task 1; do not create a new investor portfolio route.</action>
  <verify>
    <automated>npx vitest run tests/dashboard/queries.test.ts && npm run build</automated>
  </verify>
  <done>Both roles land on dashboards that expose Phase 4 metrics and transaction history while preserving existing routing and marketplace behavior.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/dashboard/queries.test.ts`
- `npm run build`
</verification>

<success_criteria>
- Cedente dashboard surfaces status, capital, financing cost, and ledger activity.
- Investor dashboard surfaces holdings, weighted yield, diversification, and history while still acting as the marketplace landing page.
- Transaction history is visible in the main dashboards for both roles.
</success_criteria>

<output>
After completion, create `.planning/phases/04-settlement-dashboards-demo-polish/04-04-SUMMARY.md`
</output>
