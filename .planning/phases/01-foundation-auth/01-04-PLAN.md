---
phase: 01-foundation-auth
plan: 04
type: execute
wave: 4
depends_on:
  - 01-02
  - 01-03
files_modified:
  - src/lib/auth/actions.ts
  - src/lib/auth/session.ts
  - src/components/auth/login-form.tsx
  - src/components/auth/signup-form.tsx
  - src/app/(public)/login/page.tsx
  - src/app/(public)/signup/page.tsx
  - src/app/auth/confirm/route.ts
  - src/app/(cedente)/dashboard/page.tsx
  - src/app/(inversor)/dashboard/page.tsx
  - tests/auth/auth-actions.test.ts
  - tests/e2e/auth-signup.spec.ts
autonomous: true
requirements:
  - USER-01
must_haves:
  truths:
    - "A new user can sign up with email/password plus role and land immediately on the correct dashboard."
    - "Profile bootstrap uses signup metadata and the shared Supabase SSR boundary."
    - "Auth happy-path verification exists for desktop and mobile widths."
  artifacts:
    - path: "src/lib/auth/actions.ts"
      provides: "Server-side signup/login/logout actions"
    - path: "src/components/auth/signup-form.tsx"
      provides: "Role-select signup UI"
    - path: "tests/e2e/auth-signup.spec.ts"
      provides: "Desktop/mobile signup flow regression coverage"
  key_links:
    - from: "src/components/auth/signup-form.tsx"
      to: "src/lib/auth/actions.ts"
      via: "form submission to server action"
      pattern: "signup action"
    - from: "src/lib/auth/actions.ts"
      to: "public.handle_new_user()"
      via: "auth.signUp metadata payload"
      pattern: "role|display_name|company_name"
---

<objective>
Implement the end-to-end auth flow with a committed default that satisfies immediate post-signup landing.

Purpose: satisfy USER-01 and remove ambiguity around hosted Supabase email confirmation.
Output: working role-select signup/login/logout flow, immediate dashboard landing, and desktop/mobile browser coverage.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
@.planning/phases/01-foundation-auth/01-02-PLAN.md
@.planning/phases/01-foundation-auth/01-03-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement server-side auth actions with immediate-login signup default</name>
  <files>src/lib/auth/actions.ts, src/lib/auth/session.ts, src/app/auth/confirm/route.ts, tests/auth/auth-actions.test.ts</files>
  <behavior>
    - Signup accepts email, password, role, display name, and optional company name.
    - The default execution path disables email confirmation for the demo project so signup can create a live session immediately.
    - Login, logout, and confirm-route recovery all share one SSR auth boundary.
  </behavior>
  <action>Implement server actions/helpers for signup, login, and logout using the shared Supabase SSR clients. **Commit to the unattended default:** configure the Supabase project for this phase with email confirmations disabled so the roadmap criterion of immediate post-signup landing is satisfied. Keep `/auth/confirm` as a fallback/recovery route, but do not treat confirm-email as the primary happy path. Update `tests/auth/auth-actions.test.ts` to verify the metadata payload contract and post-auth redirect selection.</action>
  <verify><automated>npx vitest run tests/auth/auth-actions.test.ts</automated></verify>
  <done>The auth backend path assumes immediate session creation after signup and has real automated coverage for auth action behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build auth screens and verify signup flow on desktop and mobile</name>
  <files>src/components/auth/login-form.tsx, src/components/auth/signup-form.tsx, src/app/(public)/login/page.tsx, src/app/(public)/signup/page.tsx, src/app/(cedente)/dashboard/page.tsx, src/app/(inversor)/dashboard/page.tsx, tests/e2e/auth-signup.spec.ts</files>
  <behavior>
    - Signup UI offers exactly `cedente` and `inversor` role choices.
    - Successful signup lands cedentes on the cedente dashboard and inversors on the inversor dashboard.
    - The same happy path works on desktop and mobile widths.
  </behavior>
  <action>Wire the public auth pages to the server actions with React Hook Form + Zod validation. Build lightweight but clear role-specific dashboard shells, then implement the Playwright signup/login happy-path spec. Run the spec for both desktop Chromium and `Mobile Chrome` to satisfy AGENTS.md for this user-flow change.</action>
  <verify><automated>npx playwright test tests/e2e/auth-signup.spec.ts --project=chromium && npx playwright test tests/e2e/auth-signup.spec.ts --project="Mobile Chrome"</automated></verify>
  <done>New users can create an account with a role and land on the correct dashboard shell on both desktop and mobile viewports.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/auth/auth-actions.test.ts`
- `npx playwright test tests/e2e/auth-signup.spec.ts --project=chromium`
- `npx playwright test tests/e2e/auth-signup.spec.ts --project="Mobile Chrome"`
</verification>

<success_criteria>
- USER-01 is satisfied with immediate post-signup dashboard landing.
- Email confirmation ambiguity is removed: disabled for the demo happy path, confirm route retained only as fallback.
- Auth browser coverage explicitly includes desktop and mobile widths.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-04-SUMMARY.md`
</output>
