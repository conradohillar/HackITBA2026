---
phase: 02-invoice-origination-risk-engine
plan: 04
type: execute
wave: 4
depends_on:
  - 02-02
  - 02-03
files_modified:
  - src/lib/tokenization/hash.ts
  - src/lib/tokenization/fractions.ts
  - supabase/migrations/0004_phase2_tokenize_invoice.sql
  - src/lib/invoices/actions.ts
  - src/components/invoices/tokenization-summary.tsx
  - src/app/(cedente)/cedente/invoices/[invoiceId]/page.tsx
  - tests/tokenization/hash.test.ts
  - tests/tokenization/fractions.test.ts
  - tests/invoices/tokenize-invoice.test.ts
autonomous: true
requirements:
  - INV-03
  - INV-05
must_haves:
  truths:
    - "A validated invoice is tokenized with a stable SHA-256 hash and split into exact purchasable fractions."
    - "Remainder cents are handled deterministically so the sum of fractions always matches the invoice/net amount."
    - "The invoice reaches marketplace-readable `funding` state only after tokenization artifacts exist."
  artifacts:
    - path: "src/lib/tokenization/hash.ts"
      provides: "Stable token hash generator"
    - path: "src/lib/tokenization/fractions.ts"
      provides: "Remainder-safe fraction splitter"
    - path: "supabase/migrations/0004_phase2_tokenize_invoice.sql"
      provides: "Atomic tokenization boundary"
    - path: "tests/invoices/tokenize-invoice.test.ts"
      provides: "Integration proof of validated -> tokenized -> funding transition"
  key_links:
    - from: "src/lib/tokenization/hash.ts"
      to: "src/lib/invoices/actions.ts"
      via: "tokenization payload"
      pattern: "sha256|token_hash"
    - from: "src/lib/tokenization/fractions.ts"
      to: "supabase/migrations/0004_phase2_tokenize_invoice.sql"
      via: "fraction row creation"
      pattern: "fraction_index|remainder"
    - from: "supabase/migrations/0004_phase2_tokenize_invoice.sql"
      to: "public.transition_invoice"
      via: "validated -> tokenized -> funding"
      pattern: "tokenized|funding"
---

<objective>
Finish the Phase 2 handoff boundary: tokenize validated invoices, split them into exact fractions, and move them into funding safely.

Purpose: ensure Phase 3 receives clean marketplace-ready invoices with no partial tokenization state.
Output: SHA-256 token utility, remainder-safe fraction splitter, atomic tokenization path, and UI summary of the tokenization result.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-invoice-origination-risk-engine/02-RESEARCH.md
@.planning/phases/02-invoice-origination-risk-engine/02-03-PLAN.md
@supabase/migrations/0001_foundation_schema.sql

<interfaces>
From supabase/migrations/0001_foundation_schema.sql:
```sql
create or replace function public.transition_invoice(
  p_invoice_id uuid,
  p_new_status public.invoice_status,
  p_actor_id uuid default null
) returns public.invoices
```

From research example:
```typescript
export function buildInvoiceTokenHash(payload: object) {
  return createHash('sha256').update(JSON.stringify(payload)).digest('hex')
}

export function splitInvoiceIntoFractions(total: number, count: number) {
  const base = Math.floor((total / count) * 100) / 100
  const fractions = Array.from({ length: count }, () => base)
  const assigned = Number((base * count).toFixed(2))
  fractions[count - 1] = Number((fractions[count - 1] + (total - assigned)).toFixed(2))
  return fractions
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement stable token hashing and remainder-safe fraction splitting</name>
  <files>src/lib/tokenization/hash.ts, src/lib/tokenization/fractions.ts, tests/tokenization/hash.test.ts, tests/tokenization/fractions.test.ts</files>
  <behavior>
    - Test 1: identical invoice tokenization payloads always produce the same SHA-256 hash and materially different payloads produce different hashes.
    - Test 2: fraction splitting preserves two-decimal precision and assigns any remainder to the last fraction so totals match exactly.
    - Test 3: invalid fraction counts (zero, negative, non-integer) are rejected before persistence.
  </behavior>
  <action>Per INV-03 and INV-05, create `src/lib/tokenization/hash.ts` with Node `crypto.createHash('sha256')` and `src/lib/tokenization/fractions.ts` with deterministic remainder-on-last-fraction handling. Write the unit tests first so the token and fraction contracts are explicit before any database orchestration uses them. Keep the splitter server-shared; do not duplicate the math in UI code.</action>
  <verify>
    <automated>npx vitest run tests/tokenization/hash.test.ts tests/tokenization/fractions.test.ts</automated>
  </verify>
  <done>The repo has stable tokenization primitives that guarantee exact sums and reproducible hashes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add an atomic tokenize-and-open-funding path plus tokenization summary UI</name>
  <files>supabase/migrations/0004_phase2_tokenize_invoice.sql, src/lib/invoices/actions.ts, src/components/invoices/tokenization-summary.tsx, src/app/(cedente)/cedente/invoices/[invoiceId]/page.tsx, tests/invoices/tokenize-invoice.test.ts</files>
  <behavior>
    - Test 1: a validated invoice receives `token_hash`, `net_amount`, `total_fractions`, and fraction rows in one atomic path.
    - Test 2: the invoice transitions `validated -> tokenized -> funding` only after all fractions exist.
    - Test 3: the stored fraction amounts sum exactly to the persisted total and the detail page exposes that summary to the cedente.
  </behavior>
  <action>Implement the atomic tokenization boundary recommended by research, preferably as `public.tokenize_invoice(...)` in `0004_phase2_tokenize_invoice.sql`, then call it from `src/lib/invoices/actions.ts`. Use the hash and fraction utilities from Task 1, persist the exact fraction count and amounts, and surface the result on the invoice detail screen with `tokenization-summary.tsx`. Keep state changes DB-enforced by chaining through `transition_invoice()`; do not set invoice status strings directly from application code.</action>
  <verify>
    <automated>npx vitest run tests/invoices/tokenize-invoice.test.ts tests/tokenization/hash.test.ts tests/tokenization/fractions.test.ts</automated>
  </verify>
  <done>Validated invoices can be tokenized into exact fractions and become marketplace-readable funding rows without partial writes.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/tokenization/hash.test.ts tests/tokenization/fractions.test.ts tests/invoices/tokenize-invoice.test.ts`
</verification>

<success_criteria>
- Tokenization produces a stable hash and exact fraction rows with deterministic remainder handling.
- The invoice lifecycle reaches `funding` only after the tokenization artifacts exist.
- The cedente detail view shows tokenization output ready for the marketplace handoff.
</success_criteria>

<output>
After completion, create `.planning/phases/02-invoice-origination-risk-engine/02-04-SUMMARY.md`
</output>
