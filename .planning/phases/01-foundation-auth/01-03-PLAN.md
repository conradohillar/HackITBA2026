---
phase: 01-foundation-auth
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - supabase/migrations/0001_foundation_schema.sql
  - tests/db/transition-invoice.test.ts
  - tests/db/events-append-only.test.ts
  - tests/auth/auth-actions.test.ts
autonomous: true
requirements:
  - INV-02
  - AUDIT-01
must_haves:
  truths:
    - "The six foundation tables, enums, and trigger functions exist in a versioned SQL migration."
    - "Invalid invoice transitions are rejected by the database itself."
    - "Audit/event inserts for lifecycle changes are owned by DB functions/triggers, not app code."
  artifacts:
    - path: "supabase/migrations/0001_foundation_schema.sql"
      provides: "Schema, enums, functions, indexes, and profile bootstrap trigger"
    - path: "tests/db/transition-invoice.test.ts"
      provides: "Lifecycle enforcement regression coverage"
    - path: "tests/db/events-append-only.test.ts"
      provides: "Append-only event regression coverage"
  key_links:
    - from: "supabase/migrations/0001_foundation_schema.sql"
      to: "public.invoices"
      via: "transition_invoice() FOR UPDATE function"
      pattern: "transition_invoice"
    - from: "supabase/migrations/0001_foundation_schema.sql"
      to: "public.events"
      via: "trigger/function insert path"
      pattern: "insert into public.events"
---

<objective>
Create the load-bearing database foundation for lifecycle correctness and append-only auditing.

Purpose: Phase 2+ depends on the database enforcing status rules and audit behavior without trusting application code.
Output: initial SQL migration and automated DB coverage for transition and event invariants.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
@.planning/phases/01-foundation-auth/01-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author the foundational Supabase migration</name>
  <files>supabase/migrations/0001_foundation_schema.sql</files>
  <behavior>
    - Migration creates `profiles`, `invoices`, `fractions`, `transactions`, `events`, and `bcra_cache`.
    - Money/state columns use safe SQL types (`NUMERIC(15,2)` and enums), never floats.
    - `transition_invoice()` rejects invalid jumps like `draft -> funded` and records allowed transitions.
  </behavior>
  <action>Create the first versioned SQL migration in git, not dashboard-only schema changes. Include `user_role` and `invoice_status` enums, all six tables, indexes on policy-driving columns, `handle_new_user()`, `user_role()` helper, and `transition_invoice()` with `FOR UPDATE` locking per research. Keep roles limited to `cedente` and `inversor`; do not add admin surface in Phase 1.</action>
  <verify><automated>npx supabase db lint && npx supabase migration up --local</automated></verify>
  <done>The migration applies cleanly and defines the full Phase 1 schema spine in repo-managed SQL.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement DB tests for lifecycle, audit, and profile bootstrap invariants</name>
  <files>tests/db/transition-invoice.test.ts, tests/db/events-append-only.test.ts, tests/auth/auth-actions.test.ts</files>
  <behavior>
    - Invalid status transitions fail with a DB error.
    - Allowed transitions create expected event rows.
    - Signup metadata shape required by `handle_new_user()` is covered by test expectations.
  </behavior>
  <action>Fill in the existing test files so the migration is exercised directly. Cover the roadmap invalid-transition criterion, confirm event emission on transitions, and assert the signup metadata contract that the trigger expects. Keep these tests tied to schema objects rather than duplicating lifecycle logic in helpers.</action>
  <verify><automated>npx vitest run tests/db/transition-invoice.test.ts tests/db/events-append-only.test.ts tests/auth/auth-actions.test.ts</automated></verify>
  <done>Automated tests prove the migration owns lifecycle and audit invariants and exposes the expected profile-bootstrap contract.</done>
</task>

</tasks>

<verification>
- `npx supabase db lint`
- `npx supabase migration up --local`
- `npx vitest run tests/db/transition-invoice.test.ts tests/db/events-append-only.test.ts tests/auth/auth-actions.test.ts`
</verification>

<success_criteria>
- All six foundation tables and required functions exist in a versioned migration.
- Invalid invoice transitions are rejected at the DB boundary.
- Audit/event behavior is covered by automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-03-SUMMARY.md`
</output>
