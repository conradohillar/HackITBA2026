---
phase: 01-foundation-auth
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/lib/supabase/client.ts
  - src/lib/supabase/server.ts
  - src/lib/auth/types.ts
  - src/lib/auth/schemas.ts
  - playwright.config.ts
  - vitest.config.ts
  - tests/e2e/auth-signup.spec.ts
  - tests/e2e/rbac.spec.ts
  - tests/db/transition-invoice.test.ts
  - tests/db/rls-policies.test.ts
  - tests/db/events-append-only.test.ts
  - tests/auth/auth-actions.test.ts
autonomous: true
requirements:
  - USER-01
  - USER-04
must_haves:
  truths:
    - "Shared auth and Supabase client contracts are defined once for the phase."
    - "Playwright and Vitest can run against named Phase 1 test files without config errors."
    - "Phase 1 has explicit desktop/mobile test entrypoints before any user-flow implementation begins."
  artifacts:
    - path: "src/lib/supabase/server.ts"
      provides: "Server-side Supabase SSR client factory"
    - path: "playwright.config.ts"
      provides: "Desktop and mobile Playwright projects"
    - path: "tests/auth/auth-actions.test.ts"
      provides: "Auth action regression target"
    - path: "tests/e2e/rbac.spec.ts"
      provides: "RBAC browser regression target"
  key_links:
    - from: "src/lib/supabase/server.ts"
      to: "@supabase/ssr"
      via: "createServerClient"
      pattern: "createServerClient"
    - from: "playwright.config.ts"
      to: "tests/e2e/*.spec.ts"
      via: "desktop and mobile projects"
      pattern: "chromium|Mobile Chrome"
---

<objective>
Define the shared auth/test contract layer that later implementation plans execute against.

Purpose: prevent later plans from inventing inconsistent auth helpers or skipping required desktop/mobile verification.
Output: Supabase SSR helpers, auth schemas/types, and runnable Phase 1 test scaffolds.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
@.planning/phases/01-foundation-auth/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared Supabase SSR and auth contracts</name>
  <files>src/lib/supabase/client.ts, src/lib/supabase/server.ts, src/lib/auth/types.ts, src/lib/auth/schemas.ts</files>
  <action>Create the shared browser/server Supabase client helpers following the SSR pattern from research, plus auth types/Zod schemas with exactly `cedente` and `inversor` roles. These files are the interface contracts for later plans, so keep them focused on type boundaries and client creation rather than feature logic.</action>
  <verify><automated>npm run build</automated></verify>
  <done>There is one clear Supabase SSR boundary and one clear auth schema boundary for the entire phase.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Phase 1 test harnesses with explicit desktop and mobile projects</name>
  <files>playwright.config.ts, vitest.config.ts, tests/e2e/auth-signup.spec.ts, tests/e2e/rbac.spec.ts, tests/db/transition-invoice.test.ts, tests/db/rls-policies.test.ts, tests/db/events-append-only.test.ts, tests/auth/auth-actions.test.ts</files>
  <behavior>
    - Playwright config defines at least a desktop Chromium project and a mobile project.
    - All named Phase 1 test files exist and are discoverable by the configured runners.
    - Auth logic has a dedicated unit/integration test target instead of relying on `--passWithNoTests`.
  </behavior>
  <action>Create Playwright and Vitest configs plus placeholder spec/test files matching the research validation map, including `tests/auth/auth-actions.test.ts` so auth helpers later have a real verification target. Configure Playwright for both desktop and mobile widths to satisfy AGENTS.md. The tests may start as failing/pending skeletons only if their later implementation plan explicitly fills them in; do not use any `--passWithNoTests` escape hatch.</action>
  <verify><automated>npx vitest run tests/auth/auth-actions.test.ts tests/db/transition-invoice.test.ts tests/db/rls-policies.test.ts tests/db/events-append-only.test.ts && npx playwright test --list</automated></verify>
  <done>All required Phase 1 test files exist, runners load them successfully, and Playwright explicitly exposes desktop and mobile projects.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/auth/auth-actions.test.ts tests/db/transition-invoice.test.ts tests/db/rls-policies.test.ts tests/db/events-append-only.test.ts`
- `npx playwright test --list`
</verification>

<success_criteria>
- Shared auth/test contracts exist before implementation-heavy plans begin.
- Desktop and mobile Playwright coverage is structurally required, not optional.
- No verification command can silently pass because tests are missing.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-02-SUMMARY.md`
</output>
