---
phase: 01-foundation-auth
plan: 06
type: execute
wave: 6
depends_on:
  - 01-01
  - 01-02
  - 01-03
  - 01-04
  - 01-05
files_modified:
  - supabase/seed.sql
  - src/app/api/health/route.ts
  - .planning/phases/01-foundation-auth/01-VALIDATION.md
  - .planning/STATE.md
autonomous: true
requirements:
  - INV-02
  - USER-01
  - USER-04
  - AUDIT-01
must_haves:
  truths:
    - "The full Phase 1 happy path is validated locally and on the deployed Vercel URL."
    - "Phase 1 has a concrete validation artifact in the phase directory for Nyquist and later verification workflows."
    - "Seeded baseline data lets the next phase start without manual DB setup."
  artifacts:
    - path: "supabase/seed.sql"
      provides: "Demo-safe baseline data"
    - path: "src/app/api/health/route.ts"
      provides: "Runtime and deployment smoke endpoint"
    - path: ".planning/phases/01-foundation-auth/01-VALIDATION.md"
      provides: "Phase validation record and requirement-to-check mapping"
    - path: ".planning/STATE.md"
      provides: "Updated phase execution state"
  key_links:
    - from: "Vercel deployment URL"
      to: "src/app/api/health/route.ts"
      via: "explicit deployed smoke check"
      pattern: "curl deployed /api/health"
    - from: ".planning/phases/01-foundation-auth/01-VALIDATION.md"
      to: "Phase 1 requirements"
      via: "mapped local and deployed verification evidence"
      pattern: "INV-02|USER-01|USER-04|AUDIT-01"
---

<objective>
Close Phase 1 with seeded data, explicit deployed verification, and the required validation artifact.

Purpose: ensure Phase 1 is not merely implemented locally but proven on the actual deployment path used by the demo.
Output: seed data, meaningful health check, local/deployed validation runs, and updated validation/state artifacts.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
@.planning/phases/01-foundation-auth/01-05-PLAN.md
@.planning/phases/01-foundation-auth/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create demo-safe seed data and finalize the validation artifact</name>
  <files>supabase/seed.sql, .planning/phases/01-foundation-auth/01-VALIDATION.md</files>
  <action>Populate `supabase/seed.sql` with realistic Argentine baseline records that support Phase 1 auth/RBAC verification and give Phase 2 a usable starting dataset. Update `01-VALIDATION.md` from planned state to executed evidence format: record requirement mapping, local commands, deployed commands, URLs checked, and pass/fail status. Keep the seed data within current phase scope; do not pre-build marketplace business logic here.</action>
  <verify><automated>npx supabase db reset --local</automated></verify>
  <done>Local reset yields a usable baseline dataset and the phase directory contains a concrete validation record ready for Nyquist-style review.</done>
</task>

<task type="auto">
  <name>Task 2: Run explicit local and deployed Phase 1 smoke verification</name>
  <files>src/app/api/health/route.ts, .planning/STATE.md</files>
  <action>Upgrade `/api/health` so it verifies runtime plus basic Supabase connectivity, then run the full Phase 1 suite locally and against the Vercel deployment. Local verification must include Vitest plus Playwright signup/RBAC flows on desktop and mobile. Deployed verification must explicitly hit the deployed `/api/health` URL and re-run the signup/RBAC Playwright flows with the deployed base URL. Update `.planning/STATE.md` with execution status and note any true external blocker if one remains.</action>
  <verify><automated>npx vitest run && npx playwright test tests/e2e/auth-signup.spec.ts tests/e2e/rbac.spec.ts --project=chromium && npx playwright test tests/e2e/auth-signup.spec.ts tests/e2e/rbac.spec.ts --project="Mobile Chrome" && curl -f "$DEPLOYED_URL/api/health" && PLAYWRIGHT_BASE_URL="$DEPLOYED_URL" npx playwright test tests/e2e/auth-signup.spec.ts tests/e2e/rbac.spec.ts --project=chromium && PLAYWRIGHT_BASE_URL="$DEPLOYED_URL" npx playwright test tests/e2e/auth-signup.spec.ts tests/e2e/rbac.spec.ts --project="Mobile Chrome"</automated></verify>
  <done>Phase 1 has reproducible local and deployed verification evidence, including desktop and mobile browser coverage, and project state artifacts are updated.</done>
</task>

</tasks>

<verification>
- `npx supabase db reset --local`
- `npx vitest run`
- `npx playwright test tests/e2e/auth-signup.spec.ts tests/e2e/rbac.spec.ts --project=chromium`
- `npx playwright test tests/e2e/auth-signup.spec.ts tests/e2e/rbac.spec.ts --project="Mobile Chrome"`
- `curl -f "$DEPLOYED_URL/api/health"`
- `PLAYWRIGHT_BASE_URL="$DEPLOYED_URL" npx playwright test tests/e2e/auth-signup.spec.ts tests/e2e/rbac.spec.ts --project=chromium`
- `PLAYWRIGHT_BASE_URL="$DEPLOYED_URL" npx playwright test tests/e2e/auth-signup.spec.ts tests/e2e/rbac.spec.ts --project="Mobile Chrome"`
</verification>

<success_criteria>
- The Phase 1 happy path is exercised both locally and on the deployed Vercel environment.
- `.planning/phases/01-foundation-auth/01-VALIDATION.md` exists and records requirement-level validation evidence.
- Any remaining blocker is explicitly external/human-only rather than an internal planning gap.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-06-SUMMARY.md`
</output>
