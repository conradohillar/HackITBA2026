---
phase: 01-foundation-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.ts
  - tsconfig.json
  - eslint.config.mjs
  - postcss.config.mjs
  - src/app/layout.tsx
  - src/app/globals.css
  - src/app/(public)/login/page.tsx
  - src/app/(public)/signup/page.tsx
  - src/app/(cedente)/dashboard/page.tsx
  - src/app/(inversor)/dashboard/page.tsx
  - src/app/api/health/route.ts
  - src/components/layout/app-shell.tsx
  - .env.example
autonomous: true
requirements:
  - USER-01
must_haves:
  truths:
    - "The repo contains a runnable Next.js 16 App Router app with public and role-group routes."
    - "Login, signup, and role dashboard URLs exist before auth logic is wired."
    - "The project can boot locally with environment placeholders and standard scripts."
  artifacts:
    - path: "package.json"
      provides: "Install, dev, build, lint, and test script entrypoints"
    - path: "src/app/(public)/signup/page.tsx"
      provides: "Stable signup route for later auth wiring"
    - path: "src/app/(cedente)/dashboard/page.tsx"
      provides: "Cedente landing shell"
    - path: "src/app/(inversor)/dashboard/page.tsx"
      provides: "Inversor landing shell"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/layout/app-shell.tsx"
      via: "root layout composition"
      pattern: "AppShell"
    - from: "src/app/(public)/signup/page.tsx"
      to: "future auth form wiring"
      via: "stable route file"
      pattern: "signup page"
---

<objective>
Create the smallest possible runnable application scaffold that all remaining Phase 1 plans build on.

Purpose: establish stable routes, scripts, and shell files so later plans implement auth/RBAC/schema work without inventing structure.
Output: bootable Next.js 16 app with public and role-specific route groups plus a basic health route.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the Next.js 16 app and root shell</name>
  <files>package.json, next.config.ts, tsconfig.json, eslint.config.mjs, postcss.config.mjs, src/app/layout.tsx, src/app/globals.css, src/components/layout/app-shell.tsx, .env.example</files>
  <action>Create the in-repo Next.js 16 + Tailwind 4 scaffold described in research. Include standard scripts for dev/build/lint/test, environment placeholders using `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, and a lightweight root shell component. Keep the project fullstack in one app per roadmap; do not add Prisma, Drizzle, tRPC, or a separate backend.</action>
  <verify><automated>npm install && npm run lint</automated></verify>
  <done>The repository can install dependencies and lint successfully with a standard Next.js 16 structure in place.</done>
</task>

<task type="auto">
  <name>Task 2: Add public and role-route placeholders</name>
  <files>src/app/(public)/login/page.tsx, src/app/(public)/signup/page.tsx, src/app/(cedente)/dashboard/page.tsx, src/app/(inversor)/dashboard/page.tsx, src/app/api/health/route.ts</files>
  <action>Create minimal route files for login, signup, cedente dashboard, inversor dashboard, and `/api/health`. Keep them intentionally light: clear placeholders and stable URLs only, not real auth logic yet. The health route should return a simple 200 JSON body so later plans can expand it into a runtime smoke check.</action>
  <verify><automated>npm run build</automated></verify>
  <done>The app builds with all Phase 1 public/protected route files present and ready for later implementation.</done>
</task>

</tasks>

<verification>
- `npm run lint`
- `npm run build`
</verification>

<success_criteria>
- The repo now contains runnable application code instead of planning docs only.
- Stable auth and dashboard routes exist for later plans.
- File count stays small enough that this plan is a pure scaffold step, not an overloaded implementation bundle.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-01-SUMMARY.md`
</output>
