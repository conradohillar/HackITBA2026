---
phase: 04-settlement-dashboards-demo-polish
plan: 05
type: execute
wave: 4
depends_on:
  - 04-03
  - 04-04
files_modified:
  - tests/e2e/settlement-demo.spec.ts
  - .planning/phases/04-settlement-dashboards-demo-polish/04-VALIDATION.md
  - .planning/STATE.md
autonomous: true
requirements:
  - INV-04
  - SETT-01
  - SETT-02
  - SETT-03
  - USER-02
  - USER-03
  - AUDIT-02
must_haves:
  truths:
    - "The full Karaí demo path passes on desktop and mobile from signup through settlement and dashboard/timeline review."
    - "Phase 4 records requirement-level validation evidence locally and, when accessible, against the deployed Vercel URL."
    - "If deployed verification is blocked by missing Vercel linkage/auth, the blocker is explicit and the full local fallback still passes."
  artifacts:
    - path: "tests/e2e/settlement-demo.spec.ts"
      provides: "Desktop/mobile happy-path automation for signup, origination, funding, settlement, dashboards, and timeline"
    - path: ".planning/phases/04-settlement-dashboards-demo-polish/04-VALIDATION.md"
      provides: "Requirement-to-evidence validation record for Phase 4"
    - path: ".planning/STATE.md"
      provides: "Updated project state after Phase 4 validation"
  key_links:
    - from: "tests/e2e/settlement-demo.spec.ts"
      to: "/signup|/cedente/invoices/new|/inversor/dashboard|/cedente/invoices/[invoiceId]|/inversor/invoices/[invoiceId]"
      via: "desktop/mobile happy path"
      pattern: "signup|settlement"
    - from: ".planning/phases/04-settlement-dashboards-demo-polish/04-VALIDATION.md"
      to: "INV-04|SETT-01|SETT-02|SETT-03|USER-02|USER-03|AUDIT-02"
      via: "recorded automated + MCP + deployed/local evidence"
      pattern: "Phase 4"
---

<objective>
Close Phase 4 with proof that the complete demo journey works on both local and deployed surfaces.

Purpose: make Settlement, Dashboards & Demo Polish a verified product outcome rather than a collection of isolated features.
Output: one deterministic desktop/mobile happy-path spec, Phase 4 validation evidence, and updated state for the completed milestone.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@playwright.config.ts
@tests/e2e/marketplace-funding.spec.ts
@.planning/phases/03-marketplace-funding/03-05-SUMMARY.md
@.planning/phases/04-settlement-dashboards-demo-polish/04-RESEARCH.md

<interfaces>
From playwright.config.ts:
```typescript
const baseURL = process.env.PLAYWRIGHT_BASE_URL ?? 'http://127.0.0.1:3001'
projects: [
  { name: 'chromium', use: { ...devices['Desktop Chrome'] } },
  { name: 'Mobile Chrome', use: { ...devices['Pixel 7'] } },
]
```

From tests/e2e/marketplace-funding.spec.ts:
```typescript
function createAdminClient() {
  return createClient(supabaseUrl, serviceRoleKey, { auth: { autoRefreshToken: false, persistSession: false } })
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the full settlement demo Playwright spec for desktop and mobile</name>
  <files>tests/e2e/settlement-demo.spec.ts</files>
  <action>Write one deterministic happy-path browser spec that covers the Phase 4 end state: create unique cedente and inversor users (exercise `/signup` directly if practical; otherwise seed with the admin client only for unavoidable setup and still traverse the UI flow), originate an invoice through the cedente UI, verify risk/tokenization/funding readiness, complete investor funding, trigger settlement simulation from the cedente detail page, then assert both dashboards and both invoice detail routes show the resulting metrics, transaction history, and event timeline. Run the same spec under `chromium` and `Mobile Chrome`, and keep selectors/assertions stable enough for later Vercel re-runs.</action>
  <verify>
    <automated>npx playwright test tests/e2e/settlement-demo.spec.ts --project=chromium && npx playwright test tests/e2e/settlement-demo.spec.ts --project="Mobile Chrome"</automated>
  </verify>
  <done>The repo has one repeatable desktop/mobile settlement demo spec that exercises the full product loop from signup through settlement visibility.</done>
</task>

<task type="auto">
  <name>Task 2: Run the Phase 4 gate locally, repeat with Playwright MCP, and verify deployed/Vercel behavior with fallback rules</name>
  <files>.planning/phases/04-settlement-dashboards-demo-polish/04-VALIDATION.md, .planning/STATE.md</files>
  <action>Run the full Phase 4 automated gate: `tests/invoices/settle-invoice.test.ts`, `tests/settlement/distribution.test.ts`, `tests/settlement/timeline.test.ts`, `tests/dashboard/queries.test.ts`, `npm run build`, and the new settlement demo Playwright spec on both `chromium` and `Mobile Chrome`. Then repeat the happy path with Playwright MCP once on a desktop width and once on a mobile width, fixing any discovered issue inside Phase 4 before recording success. For deployed verification, first try to resolve a Vercel target through repo linkage (`.vercel/project.json` if present), Vercel CLI, or Vercel MCP. If a deployed URL is accessible, rerun the same Playwright spec against it via `PLAYWRIGHT_BASE_URL="$DEPLOYED_URL"` and record the evidence; if deployment auth/linkage is missing or denied, explicitly document that blocker in `04-VALIDATION.md` and complete the local fallback as the required baseline instead of stopping execution. Record both the deployed requirement and the local fallback outcome in the validation artifact, and update `.planning/STATE.md` to Phase 4 complete only after the local gate passes.</action>
  <verify>
    <automated>npx vitest run tests/invoices/settle-invoice.test.ts tests/settlement/distribution.test.ts tests/settlement/timeline.test.ts tests/dashboard/queries.test.ts && npm run build && npx playwright test tests/e2e/settlement-demo.spec.ts --project=chromium && npx playwright test tests/e2e/settlement-demo.spec.ts --project="Mobile Chrome"</automated>
  </verify>
  <done>Phase 4 has requirement-level local validation, desktop/mobile MCP confirmation, and either successful deployed-Vercel evidence or a documented deploy-auth fallback result.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/invoices/settle-invoice.test.ts tests/settlement/distribution.test.ts tests/settlement/timeline.test.ts tests/dashboard/queries.test.ts`
- `npm run build`
- `npx playwright test tests/e2e/settlement-demo.spec.ts --project=chromium`
- `npx playwright test tests/e2e/settlement-demo.spec.ts --project="Mobile Chrome"`
</verification>

<success_criteria>
- Phase 4 has one automated happy path from signup to settlement visibility on desktop and mobile.
- Validation records local browser proof and the deployed-Vercel requirement with an explicit local fallback path if deployment auth is unavailable.
- `.planning/STATE.md` can move to Phase 4 complete only after the full local gate passes.
</success_criteria>

<output>
After completion, create `.planning/phases/04-settlement-dashboards-demo-polish/04-05-SUMMARY.md`
</output>
