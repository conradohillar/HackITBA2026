---
phase: 01-foundation-auth
plan: 05
type: execute
wave: 5
depends_on:
  - 01-03
  - 01-04
files_modified:
  - supabase/migrations/0002_rls_and_rbac.sql
  - proxy.ts
  - src/lib/auth/guards.ts
  - src/app/(cedente)/layout.tsx
  - src/app/(inversor)/layout.tsx
  - tests/db/rls-policies.test.ts
  - tests/e2e/rbac.spec.ts
autonomous: true
requirements:
  - USER-04
must_haves:
  truths:
    - "Unauthenticated users are redirected away from protected role routes."
    - "Cedentes cannot access inversor routes and inversors cannot access cedente routes."
    - "Database-level access rules still hold even if browser route checks are bypassed."
  artifacts:
    - path: "supabase/migrations/0002_rls_and_rbac.sql"
      provides: "RLS policies and grants for all foundation tables"
    - path: "proxy.ts"
      provides: "Optimistic session refresh and redirect boundary"
    - path: "tests/db/rls-policies.test.ts"
      provides: "DB authorization regression coverage"
    - path: "tests/e2e/rbac.spec.ts"
      provides: "Desktop/mobile browser RBAC coverage"
  key_links:
    - from: "proxy.ts"
      to: "src/app/(cedente)/layout.tsx"
      via: "protected route guard flow"
      pattern: "redirect unauthenticated/wrong role"
    - from: "supabase/migrations/0002_rls_and_rbac.sql"
      to: "public.profiles|public.invoices|public.fractions|public.transactions|public.events|public.bcra_cache"
      via: "row level security enablement and policy definitions"
      pattern: "enable row level security"
---

<objective>
Implement defense-in-depth authorization across proxy, server guards, and Postgres RLS.

Purpose: satisfy USER-04 so role separation is real security, not just navigation behavior.
Output: RLS migration, route protection helpers, and desktop/mobile RBAC verification.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
@.planning/phases/01-foundation-auth/01-03-PLAN.md
@.planning/phases/01-foundation-auth/01-04-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply and test RLS policies for all foundation tables</name>
  <files>supabase/migrations/0002_rls_and_rbac.sql, tests/db/rls-policies.test.ts</files>
  <behavior>
    - Every Phase 1 table has RLS enabled.
    - Authenticated users can only read/write rows consistent with their role and ownership.
    - Anon access cannot read or mutate protected data.
  </behavior>
  <action>Create a second migration that enables RLS on all six tables and adds explicit policies using the role/ownership patterns from research. Include `TO authenticated`, null-safe `auth.uid()` checks, and policy-driving indexes where relevant. Fill in the DB tests for anon denial, self-profile access, cedente invoice isolation, and investor marketplace visibility.</action>
  <verify><automated>npx vitest run tests/db/rls-policies.test.ts</automated></verify>
  <done>All Phase 1 tables have explicit RLS coverage and automated tests prove the main role-isolation scenarios.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add proxy redirects and verify RBAC on desktop and mobile</name>
  <files>proxy.ts, src/lib/auth/guards.ts, src/app/(cedente)/layout.tsx, src/app/(inversor)/layout.tsx, tests/e2e/rbac.spec.ts</files>
  <behavior>
    - Unauthenticated visits to protected routes redirect to login.
    - Wrong-role visits redirect away from the protected area.
    - The same guard behavior is verified on desktop and mobile widths.
  </behavior>
  <action>Implement `proxy.ts` for optimistic session refresh/redirects per current Next.js guidance, then add server-side guard helpers/layout checks close to protected data. Finish the Playwright RBAC spec and run it for both desktop Chromium and `Mobile Chrome`. Do not rely on layout-only auth; proxy + server guard + RLS must all exist.</action>
  <verify><automated>npx playwright test tests/e2e/rbac.spec.ts --project=chromium && npx playwright test tests/e2e/rbac.spec.ts --project="Mobile Chrome" && npx vitest run tests/db/rls-policies.test.ts</automated></verify>
  <done>Wrong-role and unauthenticated access are blocked in both browser tests and DB policy tests, on desktop and mobile viewports.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/db/rls-policies.test.ts`
- `npx playwright test tests/e2e/rbac.spec.ts --project=chromium`
- `npx playwright test tests/e2e/rbac.spec.ts --project="Mobile Chrome"`
</verification>

<success_criteria>
- USER-04 is enforced at proxy, server, and DB levels.
- Anon sessions cannot read or write foundation data.
- Browser verification explicitly covers desktop and mobile widths.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-05-SUMMARY.md`
</output>
