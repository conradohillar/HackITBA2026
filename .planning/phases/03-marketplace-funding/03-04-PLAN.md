---
phase: 03-marketplace-funding
plan: 04
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
  - 03-03
files_modified:
  - src/app/(inversor)/inversor/dashboard/page.tsx
  - src/app/(inversor)/inversor/invoices/[invoiceId]/page.tsx
  - src/components/marketplace/marketplace-grid.tsx
  - src/components/marketplace/marketplace-card.tsx
  - src/components/marketplace/purchase-fractions-form.tsx
autonomous: true
requirements:
  - FUND-01
  - FUND-02
  - FUND-03
  - FUND-04
must_haves:
  truths:
    - "The investor dashboard is now the marketplace landing and shows funding invoices with risk, rate, progress, and CTA."
    - "Investors can open a dedicated invoice detail screen, inspect expected return, and choose how many fractions to buy."
    - "The UI surfaces live progress and purchase outcomes without inventing a second data model."
  artifacts:
    - path: "src/app/(inversor)/inversor/dashboard/page.tsx"
      provides: "Marketplace landing route"
    - path: "src/app/(inversor)/inversor/invoices/[invoiceId]/page.tsx"
      provides: "Investor funding detail route"
    - path: "src/components/marketplace/purchase-fractions-form.tsx"
      provides: "Fraction quantity selection and purchase submit UX"
  key_links:
    - from: "src/app/(inversor)/inversor/dashboard/page.tsx"
      to: "src/lib/marketplace/queries.ts"
      via: "server-rendered marketplace listing"
      pattern: "getMarketplaceInvoices"
    - from: "src/components/marketplace/purchase-fractions-form.tsx"
      to: "src/lib/marketplace/actions.ts"
      via: "server action submit"
      pattern: "purchaseFractionsAction"
    - from: "src/app/(inversor)/inversor/invoices/[invoiceId]/page.tsx"
      to: "src/hooks/use-marketplace-realtime.ts"
      via: "live detail progress updates"
      pattern: "useMarketplaceRealtime"
---

<objective>
Assemble the investor-facing marketplace experience: dashboard listing, dedicated invoice detail route, and fraction-purchase form.

Purpose: satisfy the visible user outcomes of Phase 3 by turning the current investor dashboard shell into a live funding surface.
Output: marketplace landing UI plus invoice detail purchase flow.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/app/(inversor)/inversor/dashboard/page.tsx
@src/components/layout/dashboard-hero.tsx
@src/components/invoices/risk-badge.tsx
@.planning/phases/03-marketplace-funding/03-01-PLAN.md
@.planning/phases/03-marketplace-funding/03-03-PLAN.md

<interfaces>
From src/lib/auth/session.ts:
```typescript
export async function getCurrentAuthState() {
  return { user, profile }
}
```

From 03-01 contract:
```typescript
export type MarketplaceInvoiceCard = {
  id: string
  invoiceNumber: string
  pagadorName: string
  amount: number
  netAmount: number
  riskTier: 'A' | 'B' | 'C' | 'D'
  discountRate: number
  totalFractions: number
  fundedFractions: number
  availableFractions: number
  dueDate: string
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Turn `/inversor/dashboard` into the marketplace landing</name>
  <files>src/app/(inversor)/inversor/dashboard/page.tsx, src/components/marketplace/marketplace-grid.tsx, src/components/marketplace/marketplace-card.tsx</files>
  <action>Replace the current placeholder investor dashboard with a real marketplace landing that server-renders funding invoices via `getMarketplaceInvoices()`. Build `marketplace-grid.tsx` and `marketplace-card.tsx` to show risk tier via the existing `RiskBadge`, discount rate, due date, available/sold fraction counts, funding progress bar, realtime status chip, and a clear CTA into the detail page. Keep the page invoice-centric and lightweight: no global client store, no duplicated query logic, and no fraction table scans on initial render.</action>
  <verify>
    <automated>npm run build && npx vitest run tests/marketplace/queries.test.ts tests/marketplace/progress.test.ts tests/marketplace/realtime.test.ts</automated>
  </verify>
  <done>Investors land on a real marketplace dashboard listing funding invoices with live-ready metrics instead of placeholder copy.</done>
</task>

<task type="auto">
  <name>Task 2: Build the investor invoice detail route and purchase form</name>
  <files>src/app/(inversor)/inversor/invoices/[invoiceId]/page.tsx, src/components/marketplace/purchase-fractions-form.tsx</files>
  <action>Create a dedicated investor detail route that loads `getInvoiceFundingSnapshot(invoiceId)`, shows the shared progress/return widgets, lists the available fraction count, and renders `purchase-fractions-form.tsx` for selecting how many fractions to buy. The form must call `purchaseFractionsAction`, show success/error feedback, and update the displayed expected return total as quantity changes. Reject silent partial fulfillment: if fewer fractions remain than requested, surface the RPC/server-action error clearly.</action>
  <verify>
    <automated>npm run build && npx vitest run tests/marketplace/queries.test.ts tests/marketplace/returns.test.ts tests/marketplace/realtime.test.ts tests/invoices/fund-invoice.test.ts</automated>
  </verify>
  <done>Investors can inspect one invoice in detail, preview expected return, and submit a fraction purchase through the approved funding boundary.</done>
</task>

</tasks>

<verification>
- `npm run build`
- `npx vitest run tests/marketplace/queries.test.ts tests/marketplace/progress.test.ts tests/marketplace/returns.test.ts tests/marketplace/realtime.test.ts tests/invoices/fund-invoice.test.ts`
</verification>

<success_criteria>
- `/inversor/dashboard` behaves as the marketplace landing required by the roadmap.
- The investor detail page exists and exposes expected return plus fraction purchase UX.
- UI wiring stays on the shared query, action, and realtime contracts from earlier plans.
</success_criteria>

<output>
After completion, create `.planning/phases/03-marketplace-funding/03-04-SUMMARY.md`
</output>
