---
phase: 02-invoice-origination-risk-engine
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/lib/invoices/actions.ts
  - src/lib/invoices/queries.ts
  - src/app/(cedente)/cedente/invoices/new/page.tsx
  - src/app/(cedente)/cedente/invoices/[invoiceId]/page.tsx
  - src/components/invoices/invoice-origination-form.tsx
  - src/components/invoices/risk-summary-card.tsx
  - src/components/invoices/risk-badge.tsx
  - src/components/invoices/invoice-status-stepper.tsx
  - tests/invoices/create-invoice.test.ts
autonomous: true
requirements:
  - INV-01
  - RISK-01
  - RISK-02
  - RISK-03
  - RISK-04
  - RISK-05
  - RISK-06
must_haves:
  truths:
    - "A cedente can submit a real invoice form and the server creates a draft that moves into validating, then persists validated risk output."
    - "The invoice detail screen shows tier, discount rate, narrative, and status progression from the stored invoice data."
    - "The browser flow stays cedente-only and uses the existing server-action auth boundary instead of client-side mutation chains."
  artifacts:
    - path: "src/lib/invoices/actions.ts"
      provides: "Cedente-only origination orchestration"
    - path: "src/app/(cedente)/cedente/invoices/new/page.tsx"
      provides: "Invoice origination entry screen"
    - path: "src/app/(cedente)/cedente/invoices/[invoiceId]/page.tsx"
      provides: "Post-submit invoice detail and risk result screen"
    - path: "src/components/invoices/risk-summary-card.tsx"
      provides: "Risk result presentation"
  key_links:
    - from: "src/components/invoices/invoice-origination-form.tsx"
      to: "src/lib/invoices/actions.ts"
      via: "server action submit"
      pattern: "submitInvoice|formAction"
    - from: "src/lib/invoices/actions.ts"
      to: "public.transition_invoice"
      via: "draft -> validating -> validated"
      pattern: "transition_invoice"
    - from: "src/app/(cedente)/cedente/invoices/[invoiceId]/page.tsx"
      to: "src/lib/invoices/queries.ts"
      via: "stored invoice + risk snapshot"
      pattern: "risk_tier|discount_rate|risk_explanation"
---

<objective>
Wire the cedente-facing origination experience: one server-side submit flow that creates the invoice, validates risk, and renders the result pages with stored data.

Purpose: turn the Phase 2 contracts and risk engine into an actual user path that can be exercised in the browser.
Output: server actions and queries for invoice origination plus the new and detail cedente pages with risk/status UI components.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-invoice-origination-risk-engine/02-RESEARCH.md
@.planning/phases/01-foundation-auth/01-04-SUMMARY.md
@.planning/phases/01-foundation-auth/01-05-SUMMARY.md
@src/lib/auth/guards.ts
@src/lib/auth/actions.ts

<interfaces>
From src/lib/auth/guards.ts:
```typescript
export async function requireRole(expectedRole: AuthRole) {
  // redirects unauthenticated users to /login
  // redirects wrong-role users to their dashboard
}
```

From src/lib/auth/actions.ts:
```typescript
export type AuthActionResult = {
  status: 'success' | 'error';
  message?: string;
  redirectTo?: string;
  fieldErrors?: Record<string, string>;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the cedente origination server action and stored risk persistence</name>
  <files>src/lib/invoices/actions.ts, src/lib/invoices/queries.ts, tests/invoices/create-invoice.test.ts</files>
  <behavior>
    - Test 1: a cedente submit creates a `draft` invoice row with description, transitions it to `validating`, runs BCRA lookup + deterministic scoring + optional narrative, then persists the validated result.
    - Test 2: if the live BCRA call fails, the action falls back per the deterministic strategy and still completes without a user-visible crash.
    - Test 3: wrong-role or unauthenticated execution is rejected before any invoice write occurs.
  </behavior>
  <action>In `src/lib/invoices/actions.ts`, implement the single happy-path server action recommended by research: create draft invoice, call `transition_invoice()` to `validating`, fetch/cache/normalize BCRA data, compute deterministic tier/rate, optionally attach the LLM narrative, persist `bcra_data`, `risk_tier`, `discount_rate`, and `risk_explanation`, then transition to `validated`. Use `createSupabaseServerClient()` and the existing cedente auth boundary from Phase 1; do not move mutations into client code. Add the supporting read helper in `src/lib/invoices/queries.ts` for the invoice detail page.</action>
  <verify>
    <automated>npx vitest run tests/invoices/create-invoice.test.ts tests/risk/bcra-client.test.ts tests/risk/fallbacks.test.ts</automated>
  </verify>
  <done>Cedente-originated invoice submission is fully server-side, role-guarded, and stores a validated risk snapshot even when fallback mode is required.</done>
</task>

<task type="auto">
  <name>Task 2: Build the new invoice page and cedente origination form</name>
  <files>src/app/(cedente)/cedente/invoices/new/page.tsx, src/components/invoices/invoice-origination-form.tsx, src/components/invoices/risk-badge.tsx</files>
  <action>Build the `/cedente/invoices/new` screen around the shared invoice schema and server action. Follow the Phase 1 react-hook-form + zod + server-action pattern, keep the form cedente-only via the existing layout/guard system, and expose fields for payer CUIT, payer name, invoice number, face value, due date, issue date, description, and configurable fraction count. Reuse `risk-badge.tsx` as the canonical A/B/C/D visual contract to keep later marketplace cards consistent.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The new cedente page renders a typed origination form that submits through the server action and is ready for browser validation.</done>
</task>

<task type="auto">
  <name>Task 3: Build the invoice detail screen for stored risk results and lifecycle state</name>
  <files>src/app/(cedente)/cedente/invoices/[invoiceId]/page.tsx, src/components/invoices/risk-summary-card.tsx, src/components/invoices/invoice-status-stepper.tsx</files>
  <action>Render the post-submit invoice detail page from stored invoice/query data, not transient client state. Show the persisted risk tier, discount rate, LLM narrative or fallback explanation, key BCRA evidence, and the enforced status progression `draft → validating → validated` so tokenization can plug into the same screen later. Keep the UI focused on the Phase 2 happy path; do not add marketplace or settlement concerns here.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Cedentes can land on an invoice detail page that accurately reflects stored validation results and lifecycle status.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/invoices/create-invoice.test.ts tests/risk/bcra-client.test.ts tests/risk/fallbacks.test.ts`
- `npm run build`
</verification>

<success_criteria>
- A single cedente submit path creates, validates, and stores invoice risk data through server actions.
- The browser pages for `/cedente/invoices/new` and `/cedente/invoices/[invoiceId]` exist and use stored data contracts.
- The implemented flow honors the existing RBAC/layout patterns instead of inventing a second architecture.
</success_criteria>

<output>
After completion, create `.planning/phases/02-invoice-origination-risk-engine/02-03-SUMMARY.md`
</output>
