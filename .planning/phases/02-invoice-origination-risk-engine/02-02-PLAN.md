---
phase: 02-invoice-origination-risk-engine
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/lib/risk/deterministic.ts
  - src/lib/risk/pricing.ts
  - src/lib/risk/llm.ts
  - tests/risk/deterministic-engine.test.ts
  - tests/risk/pricing.test.ts
  - tests/risk/fallbacks.test.ts
  - tests/risk/llm-narrative.test.ts
autonomous: true
requirements:
  - RISK-01
  - RISK-02
  - RISK-03
  - RISK-05
  - RISK-06
must_haves:
  truths:
    - "Risk tier and discount rate are computed deterministically from payer data even when live BCRA or LLM calls fail."
    - "The LLM contributes only a human-readable narrative backed by known evidence, never the authoritative score."
    - "Fallback execution is explicit and test-covered rather than silently breaking the validation flow."
  artifacts:
    - path: "src/lib/risk/deterministic.ts"
      provides: "Authoritative tier and fallback scoring engine"
    - path: "src/lib/risk/pricing.ts"
      provides: "Tier + maturity discount-rate calculation"
    - path: "src/lib/risk/llm.ts"
      provides: "Validated narrative generation wrapper"
    - path: "tests/risk/fallbacks.test.ts"
      provides: "Regression coverage for degraded BCRA/LLM scenarios"
  key_links:
    - from: "src/lib/risk/deterministic.ts"
      to: "src/lib/risk/pricing.ts"
      via: "tier and days-to-maturity inputs"
      pattern: "discountRate|tier"
    - from: "src/lib/risk/llm.ts"
      to: "deterministic outputs"
      via: "narrative prompt inputs only"
      pattern: "evidence|tier|discountRate"
---

<objective>
Implement the authoritative risk engine: deterministic scoring and pricing first, LLM narrative second, with fallback behavior guaranteed by tests.

Purpose: satisfy the core risk requirements without making external services a correctness boundary.
Output: deterministic scorer, pricing utility, validated LLM narrative wrapper, and automated fallback coverage.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-invoice-origination-risk-engine/02-RESEARCH.md
@.planning/phases/02-invoice-origination-risk-engine/02-01-PLAN.md
@package.json

<interfaces>
From .planning/phases/02-invoice-origination-risk-engine/02-RESEARCH.md:
```typescript
type DeterministicRiskResult = {
  tier: 'A' | 'B' | 'C' | 'D';
  discountRate: number;
  signals: string[];
  fallbackUsed: boolean;
};
```

Expected narrative shape:
```typescript
type RiskNarrative = {
  explanation: string;
  evidence: string[];
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build deterministic tier, pricing, and degraded-path fallback coverage</name>
  <files>src/lib/risk/deterministic.ts, src/lib/risk/pricing.ts, tests/risk/deterministic-engine.test.ts, tests/risk/pricing.test.ts, tests/risk/fallbacks.test.ts</files>
  <behavior>
    - Test 1: `situacion` maps to tier A/B/C/D using the research rubric, then bounded rate adjustments apply from days-to-maturity and adverse signals.
    - Test 2: stale cache or missing live BCRA data still yields a deterministic tier/rate pair with `fallbackUsed: true`.
    - Test 3: no branch returns a user-visible crash just because BCRA or LLM enrichment is unavailable.
  </behavior>
  <action>Implement the authoritative deterministic engine per RISK-01, RISK-02, RISK-03, and RISK-06. Put the tier rubric and adverse-signal handling in `src/lib/risk/deterministic.ts`, keep rate math in `src/lib/risk/pricing.ts`, and write the fallback tests first so the code proves cache-first degraded execution. Honor the research guidance that deterministic scoring is the source of truth and that fallback is explicit, not best-effort guesswork.</action>
  <verify>
    <automated>npx vitest run tests/risk/deterministic-engine.test.ts tests/risk/pricing.test.ts tests/risk/fallbacks.test.ts</automated>
  </verify>
  <done>The repo has a deterministic scorer that always returns tier/rate output, including BCRA degradation paths, with focused regression coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add schema-validated LLM narrative generation without changing the score authority</name>
  <files>src/lib/risk/llm.ts, tests/risk/llm-narrative.test.ts</files>
  <behavior>
    - Test 1: the LLM wrapper only accepts deterministic tier/rate plus known evidence as input and returns a schema-validated narrative object.
    - Test 2: invalid, timed-out, or contradictory model output falls back to a deterministic explanation string while preserving the authoritative score.
    - Test 3: evidence strings in the final narrative come from supplied facts instead of invented claims.
  </behavior>
  <action>Per RISK-05 and RISK-06, implement `src/lib/risk/llm.ts` using `ai` + `@ai-sdk/openai` structured generation so the model produces narrative-only output. Validate with zod, cap evidence count, and return a deterministic fallback explanation if generation fails or the payload is invalid. Do not let this module compute or override the risk tier or discount rate.</action>
  <verify>
    <automated>npx vitest run tests/risk/llm-narrative.test.ts</automated>
  </verify>
  <done>LLM narrative generation is typed, evidence-backed, and fully optional from a correctness perspective.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/risk/deterministic-engine.test.ts tests/risk/pricing.test.ts tests/risk/fallbacks.test.ts`
- `npx vitest run tests/risk/llm-narrative.test.ts`
</verification>

<success_criteria>
- Deterministic scoring and pricing satisfy the authoritative risk requirements without depending on external success.
- LLM narrative generation is layered on top, never underneath, the deterministic engine.
- Failure modes for BCRA and LLM are regression-tested before UI wiring starts.
</success_criteria>

<output>
After completion, create `.planning/phases/02-invoice-origination-risk-engine/02-02-SUMMARY.md`
</output>
